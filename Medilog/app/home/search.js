import { StyleSheet, Text, View } from "react-native";
import React from "react";

const blog = () => {
  return (
    <View>
      <Text>blog</Text>
    </View>
  );
};

export default blog;

const styles = StyleSheet.create({});
